services:
  gateway:
    build:
      context: ..
      dockerfile: Dockerfile
    env_file:
      - ../.env
    ports:
      - "8001:8001"
    volumes:
      - ../data:/app/data
    restart: unless-stopped
